/**
 * cluster.js — NOT USED
 *
 * PM2 handles clustering via ecosystem.config.js (exec_mode: 'cluster').
 * Do NOT run this file directly — use:
 *
 *   Production:  npm start          (pm2-runtime via ecosystem.config.js)
 *   Dev:         npm run dev        (nodemon server.js, single process)
 *   Reload:      npm run reload     (zero-downtime reload via PM2)
 */
